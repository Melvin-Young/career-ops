# Application Artifacts are versioned derivatives

Role-specific resumes and cover letters will be editable, versioned Application Artifacts derived from the canonical Career Profile and relevant Market Evidence. Tailoring never overwrites the Career Profile, and rendering or export requires an explicitly approved artifact version; this preserves reusable evidence while allowing each Opportunity to have a deliberate, auditable application package.
